export const metadata = {
  title: "Custom Systems, Built to Spec | Northspec Studio",
  description:
    "For teams that need tailored software, automation, or platforms designed around their exact workflows. Custom builds typically start at $20,000+.",
};

export default function BuiltToSpecLayout({ children }) {
  return children;
}
