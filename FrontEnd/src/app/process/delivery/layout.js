export const metadata = {
  title: "Delivery Phase | Northspec Studio",
  description: "How we test, deploy, and hand off production-ready systems.",
};

export default function DeliveryLayout({ children }) {
  return <>{children}</>;
}
